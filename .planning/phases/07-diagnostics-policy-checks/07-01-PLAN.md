---
phase: 07-diagnostics-policy-checks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/simulation_logs/expectations.py
  - tests/simulation_logs/matching.py
  - tests/simulation_logs/diagnostics.py
  - tests/simulation_logs/__init__.py
  - tests/test_transfer_mapping.py
autonomous: true

must_haves:
  truths:
    - "Transfer validation failures include semantic expected vs observed summaries."
    - "Coverage metrics report CSV row coverage for each fixture run."
    - "Distribution rows count as single CSV actions in coverage."
  artifacts:
    - path: "tests/simulation_logs/diagnostics.py"
      provides: "Coverage computation and formatted diagnostic reports"
    - path: "tests/simulation_logs/expectations.py"
      provides: "ExpectedTransfer rows include CSV row indexing"
      contains: "row_index"
    - path: "tests/simulation_logs/matching.py"
      provides: "MatchResult exposes missing/mismatched ExpectedTransfer objects"
      contains: "missing_expected"
    - path: "tests/test_transfer_mapping.py"
      provides: "Fixture-backed diagnostics assertions and coverage checks"
  key_links:
    - from: "tests/test_transfer_mapping.py"
      to: "tests/simulation_logs/diagnostics.py"
      via: "format/coverage helper usage"
      pattern: "diagnostics"
    - from: "tests/simulation_logs/matching.py"
      to: "tests/simulation_logs/expectations.py"
      via: "row_index and ExpectedTransfer references"
      pattern: "row_index"
    - from: "tests/simulation_logs/diagnostics.py"
      to: "tests/simulation_logs/matching.py"
      via: "MatchResult missing/mismatched expected transfers"
      pattern: "missing_expected"
---

<objective>
Add semantic transfer diagnostics and CSV row coverage metrics to fixture-backed validation.

Purpose: Provide actionable failure reporting and coverage visibility for DIAG-02/DIAG-03.
Output: Diagnostics helpers, enhanced MatchResult data, and updated mapping tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-diagnostics-policy-checks/07-CONTEXT.md
@.planning/phases/07-diagnostics-policy-checks/07-RESEARCH.md
@tests/simulation_logs/expectations.py
@tests/simulation_logs/matching.py
@tests/test_transfer_mapping.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSV row indexing and match metadata</name>
  <files>tests/simulation_logs/expectations.py; tests/simulation_logs/matching.py</files>
  <action>
Add a `row_index: int | None` field to `ExpectedTransfer` and populate it per CSV row (use the 1-based CSV row index, skipping HOME rows). Update `build_expected_transfers` to set `row_index` for both standard and distribution rows.

Extend `MatchResult` to include `missing_expected` and `mismatched_expected` lists of `ExpectedTransfer` objects (in addition to the existing string summaries). Populate these lists wherever missing/mismatch strings are added, including distribution-group and split-transfer paths. Update the formatted report lines to include row indexing in the label (e.g., prefix with `row {row_index}`) while preserving group_id labeling.

Keep the existing `missing`, `mismatched`, and `extra` string lists for backward compatibility.
  </action>
  <verify>uv run pytest tests/test_transfer_mapping.py</verify>
  <done>`ExpectedTransfer` carries row index; `MatchResult` surfaces missing/mismatched ExpectedTransfer objects and report lines include row context.</done>
</task>

<task type="auto">
  <name>Task 2: Add diagnostics helpers and coverage reporting</name>
  <files>tests/simulation_logs/diagnostics.py; tests/simulation_logs/__init__.py; tests/test_transfer_mapping.py</files>
  <action>
Create `tests/simulation_logs/diagnostics.py` with helpers to compute CSV-row coverage and to format a semantic failure report. Coverage should compute total/covered/missing CSV rows using `row_index` and `MatchResult.missing_expected` + `mismatched_expected`, treating distribution rows as a single row via their shared row_index.

Expose diagnostics helpers in `tests/simulation_logs/__init__.py`. Update `tests/test_transfer_mapping.py` to include coverage metrics in assertion output and to assert 100% coverage for fixtures with successful matches. Ensure the failure message combines `MatchResult.report()` with coverage summary so failures explain expected vs observed and coverage impact.
  </action>
  <verify>uv run pytest tests/test_transfer_mapping.py</verify>
  <done>Transfer mapping tests report coverage metrics and include semantic expected-vs-observed diagnostics on failure.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_transfer_mapping.py`
</verification>

<success_criteria>
- Coverage metrics are reported by CSV row for fixture-backed transfer validation.
- Failed matches include row-aware semantic diagnostics (expected vs observed) in test output.
</success_criteria>

<output>
After completion, create `.planning/phases/07-diagnostics-policy-checks/07-01-SUMMARY.md`
</output>
