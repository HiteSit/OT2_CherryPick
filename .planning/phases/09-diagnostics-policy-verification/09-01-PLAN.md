---
phase: 09-diagnostics-policy-verification
plan: 01
type: execute
wave: 2
depends_on: [08-07]
files_modified:
  - tests/unit/transfer_mapping/test_transfer_mapping.py
  - tests/unit/transfer_mapping/test_transfer_policies.py
autonomous: true

must_haves:
  truths:
    - "Transfer matching failures emit semantic reports with missing/mismatched/extra entries."
    - "CSV row coverage metrics are reported alongside transfer diagnostics."
    - "Policy checks surface tip reuse, mix, and air gap mismatches with row context."
  artifacts:
    - path: "tests/unit/transfer_mapping/test_transfer_mapping.py"
      provides: "Diagnostics assertions for semantic transfer reports and row coverage"
    - path: "tests/unit/transfer_mapping/test_transfer_policies.py"
      provides: "Policy mismatch test coverage for tip reuse, mix, and air gap intent"
  key_links:
    - from: "tests/unit/transfer_mapping/test_transfer_mapping.py"
      to: "tests/unit/simulation_logs/diagnostics.py"
      via: "format_transfer_report and format_row_coverage"
      pattern: "format_transfer_report\(|format_row_coverage\("
    - from: "tests/unit/transfer_mapping/test_transfer_policies.py"
      to: "tests/unit/simulation_logs/policies.py"
      via: "evaluate_policies"
      pattern: "evaluate_policies\("
    - from: "tests/unit/transfer_mapping/test_transfer_policies.py"
      to: "tests/unit/simulation_logs/normalize.py"
      via: "NormalizedTipPickupEvent/NormalizedTipDropEvent"
      pattern: "NormalizedTip(Pickup|Drop)Event"
    - from: "tests/unit/transfer_mapping/test_transfer_policies.py"
      to: "tests/unit/simulation_logs/normalize.py"
      via: "NormalizedMixEvent"
      pattern: "NormalizedMixEvent"
---

<objective>
Verify diagnostic reporting and policy checks meet milestone requirements with explicit failure-path assertions.

Purpose: Close DIAG-02/DIAG-03/POL-01 by proving tests emit semantic transfer reports, coverage metrics, and policy mismatch errors.
Output: Updated transfer mapping and policy tests with explicit diagnostic and mismatch assertions.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-diagnostics-policy-verification/09-RESEARCH.md

@tests/unit/transfer_mapping/test_transfer_mapping.py
@tests/unit/transfer_mapping/test_transfer_policies.py
@tests/unit/simulation_logs/diagnostics.py
@tests/unit/simulation_logs/policies.py
@tests/unit/simulation_logs/normalize.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert semantic transfer diagnostics and row coverage on failure</name>
  <files>tests/unit/transfer_mapping/test_transfer_mapping.py</files>
  <action>
Add a failure-path test that intentionally injects a mismatch and an extra transfer event for a fixture-backed run.
Use `format_transfer_report(match, expectations)` to generate the report and assert it contains the missing/mismatched
section plus an `Extra:` block and a `Coverage:` summary. Build the extra event using a `NormalizedDispenseEvent`
with a sequence_index after existing events to guarantee it is treated as extra. Keep `allow_extra_events` at the
default `False` to enforce the Phase 6 decision on extra events.
  </action>
  <verify>uv run pytest tests/unit/transfer_mapping/test_transfer_mapping.py -q</verify>
  <done>Diagnostics tests confirm semantic transfer reports include extra/missing/mismatched details and coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Add policy mismatch tests for tip reuse, mix, and air gap intent</name>
  <files>tests/unit/transfer_mapping/test_transfer_policies.py</files>
  <action>
Add three negative policy tests using temp CSVs to confirm mismatches raise errors. First, create a CSV with
`Tip Action` values and evaluate policies with no tip pickup/drop events so `tip_reuse` errors are emitted; assert
the policy name appears in `result.errors` and in `result.summary()`. Second, create a CSV with `Mix Volume` intent,
provide a mismatched `NormalizedMixEvent` (wrong destination well) so mix evidence is missing for the intended row,
and assert at least one `mix` error with a row index in the message. Third, create a CSV with `Air Gap` intent,
build expected transfers, and pass a MatchResult with those transfers in `missing_expected` while supplying a
minimal events list that includes a pickup/drop pair to avoid tip reuse errors. Assert at least one `air_gap`
error and that its message mentions the row index.
  </action>
  <verify>uv run pytest tests/unit/transfer_mapping/test_transfer_policies.py -q</verify>
  <done>Policy tests fail on tip reuse, mix, and air gap mismatches with row-context errors.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/transfer_mapping -q`
</verification>

<success_criteria>
- Diagnostic failure tests assert semantic transfer reports include extra/missing/mismatched sections and coverage.
  - Policy mismatch tests prove tip reuse, mix, and air gap intent failures surface as errors with row context.
</success_criteria>

<output>
After completion, create `.planning/phases/09-diagnostics-policy-verification/09-01-SUMMARY.md`
</output>
