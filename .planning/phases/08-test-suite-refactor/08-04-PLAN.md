---
phase: 08-test-suite-refactor
plan: 04
type: execute
wave: 3
depends_on: [08-03]
files_modified:
  - tests/unit/__init__.py
  - tests/unit/simulation_logs/__init__.py
  - tests/unit/simulation_logs/models.py
  - tests/unit/simulation_logs/parse.py
  - tests/unit/simulation_logs/normalize.py
  - tests/unit/simulation_logs/adapters/__init__.py
  - tests/unit/simulation_logs/adapters/v8_7_0.py
  - tests/support/simulation.py
  - tests/simulation_logs
autonomous: true

must_haves:
  truths:
    - "Core simulation log parsing modules live under the unit test tree."
    - "Adapters and normalization modules import correctly from the unit package."
    - "Legacy core parsing files in tests/simulation_logs are removed."
  artifacts:
    - path: "tests/unit/simulation_logs/parse.py"
      provides: "Parse fixtures from the unit package location"
    - path: "tests/unit/simulation_logs/adapters/v8_7_0.py"
      provides: "Simulator adapter under unit package"
  key_links:
    - from: "tests/unit/simulation_logs/parse.py"
      to: "tests/support/paths.py"
      via: "shared fixture/settings roots"
      pattern: "support\.paths"
    - from: "tests/support/simulation.py"
      to: "tests/unit/simulation_logs/normalize.py"
      via: "load_settings_profile"
      pattern: "unit\.simulation_logs\.normalize"
---

<objective>
Move core simulation log parsing modules and adapters into the unit test package.

Purpose: Align parser/adapter code with the unit test tree before moving test modules.
Output: Unit package for simulation log models, parsing, normalization, and adapters.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-test-suite-refactor/08-CONTEXT.md
@.planning/phases/08-test-suite-refactor/08-RESEARCH.md

@tests/simulation_logs/models.py
@tests/simulation_logs/parse.py
@tests/simulation_logs/normalize.py
@tests/simulation_logs/adapters/v8_7_0.py
@tests/support/simulation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move core parsing modules and adapters to unit package</name>
  <files>
tests/unit/__init__.py, tests/unit/simulation_logs/__init__.py,
tests/unit/simulation_logs/models.py, tests/unit/simulation_logs/parse.py,
tests/unit/simulation_logs/normalize.py, tests/unit/simulation_logs/adapters/__init__.py,
tests/unit/simulation_logs/adapters/v8_7_0.py, tests/simulation_logs
  </files>
  <action>
Create `tests/unit/` and `tests/unit/simulation_logs/` (with `__init__.py` files). Move the core parsing
modules (`models.py`, `parse.py`, `normalize.py`) and the adapter package (`adapters/__init__.py`,
`adapters/v8_7_0.py`) from `tests/simulation_logs/` into the new unit package. Update imports inside the moved
modules to reference `tests.unit.simulation_logs` and `tests.support.paths` instead of the old package path. Remove
the moved files from `tests/simulation_logs/`.
  </action>
  <verify>uv run python -c "from tests.unit.simulation_logs import parse; print(parse.DEFAULT_SIMULATOR_VERSION)"</verify>
  <done>Core simulation log parsing modules import correctly from `tests.unit.simulation_logs`.</done>
</task>

<task type="auto">
  <name>Task 2: Update support helpers for new unit module paths</name>
  <files>tests/support/simulation.py</files>
  <action>
Update `tests/support/simulation.py` imports to reference `tests.unit.simulation_logs.parse` and
`tests.unit.simulation_logs.normalize` after the move. Leave the expectations import pointing to
`tests.simulation_logs.expectations` for now. Ensure helper function behavior remains unchanged; only update
import paths and any module references required for the new location.
  </action>
  <verify>uv run python -c "from tests.support import simulation; print(simulation.parse_fixture_entry.__name__)"</verify>
  <done>Support helpers use unit simulation log modules without import errors.</done>
</task>

</tasks>

<verification>
- `uv run python -c "from tests.unit.simulation_logs import parse; print(parse.DEFAULT_SIMULATOR_VERSION)"`
</verification>

<success_criteria>
- Core simulation log parsing modules live under `tests/unit/simulation_logs`.
- Support helpers reference the new unit module paths.
</success_criteria>

<output>
After completion, create `.planning/phases/08-test-suite-refactor/08-04-SUMMARY.md`
</output>
