---
phase: 08-test-suite-refactor
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - tests/support/simulation.py
  - tests/test_simulation_log_parsing.py
  - tests/test_transfer_mapping.py
  - tests/test_transfer_expectations.py
  - tests/test_transfer_policies.py
autonomous: true

must_haves:
  truths:
    - "Shared helpers exist for parser setup and fixture normalization."
    - "Simulation log and transfer-mapping tests use support helpers instead of local repo root math."
    - "Fixture normalization helpers reuse manifest entries for expected transfers and parsed events."
  artifacts:
    - path: "tests/support/simulation.py"
      provides: "load_settings_profile, build_expected_transfers_for_entry, parse_fixture_entry"
    - path: "tests/test_simulation_log_parsing.py"
      provides: "Parsing tests wired to shared support helpers"
  key_links:
    - from: "tests/support/simulation.py"
      to: "tests/simulation_logs/parse.py"
      via: "parse_fixture_entry"
      pattern: "simulation_logs\.parse"
    - from: "tests/support/simulation.py"
      to: "tests/simulation_logs/normalize.py"
      via: "load_settings_profile"
      pattern: "simulation_logs\.normalize"
---

<objective>
Add shared parser setup and fixture normalization helpers in tests/support and rewire existing tests.

Purpose: Provide a single support surface for parser setup and fixture normalization before moving tests.
Output: `tests/support/simulation.py` and updated test modules using the shared helpers.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-test-suite-refactor/08-CONTEXT.md
@.planning/phases/08-test-suite-refactor/08-RESEARCH.md

@tests/simulation_logs/parse.py
@tests/simulation_logs/normalize.py
@tests/simulation_logs/expectations.py
@tests/test_simulation_log_parsing.py
@tests/test_transfer_mapping.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared parser setup + fixture normalization helpers</name>
  <files>tests/support/simulation.py</files>
  <action>
Create `tests/support/simulation.py` to centralize parser setup and fixture normalization:
- `load_settings_profile(profile: str)` -> loads TOML via `tests.simulation_logs.normalize.load_settings` using
  `tests.support.paths.settings_profile_path(profile)`.
- `resolve_fixture_csv(entry: FixtureEntry)` -> returns `Path` using `tests.support.paths.repo_root()` + entry.csv_path.
- `build_expected_transfers_for_entry(entry: FixtureEntry)` -> calls `tests.simulation_logs.expectations.build_expected_transfers`.
- `parse_fixture_entry(entry: FixtureEntry)` -> calls `tests.simulation_logs.parse.parse_fixture(entry.fixture_id)`.
- `build_fixture_context(entry: FixtureEntry)` -> returns expected_transfers, parsed_result, csv_path, settings.
Export the helpers in `__all__`. Do not change behavior beyond consolidating helper logic.
  </action>
  <verify>uv run python -c "from tests.support import simulation; print(simulation.load_settings_profile('single_X1').keys())"</verify>
  <done>`tests/support/simulation.py` exposes shared parser setup + fixture normalization helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire simulation/transfer tests to support helpers</name>
  <files>
tests/test_simulation_log_parsing.py, tests/test_transfer_mapping.py,
tests/test_transfer_expectations.py, tests/test_transfer_policies.py
  </files>
  <action>
Update the listed tests to import helper functions from `tests.support.simulation` and remove local `_repo_root`,
`_load_settings_profile`, and fixture-specific path helpers. Use `build_expected_transfers_for_entry`,
`parse_fixture_entry`, `resolve_fixture_csv`, and `load_settings_profile` where applicable, keeping existing
assertions and behavior unchanged.
  </action>
  <verify>uv run pytest tests/test_simulation_log_parsing.py -q</verify>
  <done>Tests use shared support helpers with no local repo root/path duplication.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_simulation_log_parsing.py -q`
</verification>

<success_criteria>
- Shared parser setup + fixture normalization helpers exist in `tests/support/simulation.py`.
- Simulation log and transfer-mapping tests use the shared helpers for settings and fixture context.
</success_criteria>

<output>
After completion, create `.planning/phases/08-test-suite-refactor/08-03-SUMMARY.md`
</output>
