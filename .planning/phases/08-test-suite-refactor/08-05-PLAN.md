---
phase: 08-test-suite-refactor
plan: 05
type: execute
wave: 4
depends_on: [08-04]
files_modified:
  - tests/unit/simulation_logs/expectations.py
  - tests/unit/simulation_logs/matching.py
  - tests/unit/simulation_logs/diagnostics.py
  - tests/unit/simulation_logs/policies.py
  - tests/support/simulation.py
  - tests/simulation_logs
autonomous: true

must_haves:
  truths:
    - "Simulation log expectations, matching, diagnostics, and policies live under the unit test tree."
    - "Module imports resolve within the unit simulation_logs package."
    - "Legacy tests/simulation_logs modules are removed."
  artifacts:
    - path: "tests/unit/simulation_logs/expectations.py"
      provides: "Expected transfer construction under unit package"
    - path: "tests/unit/simulation_logs/matching.py"
      provides: "Transfer matching logic under unit package"
  key_links:
    - from: "tests/unit/simulation_logs/matching.py"
      to: "tests/unit/simulation_logs/normalize.py"
      via: "normalized event imports"
      pattern: "unit\.simulation_logs\.normalize"
---

<objective>
Move simulation log matching, diagnostics, expectations, and policies modules into the unit package.

Purpose: Keep all simulation log domain logic inside the unit test tree before moving test files.
Output: Unit package modules for matching, diagnostics, expectations, and policies.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-test-suite-refactor/08-CONTEXT.md
@.planning/phases/08-test-suite-refactor/08-RESEARCH.md

@tests/simulation_logs/expectations.py
@tests/simulation_logs/matching.py
@tests/simulation_logs/diagnostics.py
@tests/simulation_logs/policies.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move matching, diagnostics, expectations, and policies modules</name>
  <files>
tests/unit/simulation_logs/expectations.py, tests/unit/simulation_logs/matching.py,
tests/unit/simulation_logs/diagnostics.py, tests/unit/simulation_logs/policies.py, tests/support/simulation.py,
tests/simulation_logs
  </files>
  <action>
Move `expectations.py`, `matching.py`, `diagnostics.py`, and `policies.py` from `tests/simulation_logs/` into
`tests/unit/simulation_logs/`. Update imports within these modules to reference the new unit package paths for
models, normalize, and other simulation log utilities. Remove the original files and any now-empty
`tests/simulation_logs/` directory. Update `tests/support/simulation.py` to import expectations from
`tests.unit.simulation_logs.expectations` now that the module has moved.
  </action>
  <verify>uv run pytest tests/unit/simulation_logs -q</verify>
  <done>Simulation log domain modules run from `tests/unit/simulation_logs` without import errors.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/simulation_logs -q`
</verification>

<success_criteria>
- Simulation log domain modules live under `tests/unit/simulation_logs` and import from unit paths.
- Legacy `tests/simulation_logs` package is removed after the move.
</success_criteria>

<output>
After completion, create `.planning/phases/08-test-suite-refactor/08-05-SUMMARY.md`
</output>
