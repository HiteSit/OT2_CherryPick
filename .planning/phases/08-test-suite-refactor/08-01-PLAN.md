---
phase: 08-test-suite-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/support/__init__.py
  - tests/support/paths.py
  - tests/support/fixtures.py
  - tests/simulation_logs/parse.py
  - tests/e2e/test_simulation_log_fixtures.py
  - tests/fixtures/simulation/capture.py
  - tests/fixtures/simulation/__init__.py
autonomous: true

must_haves:
  truths:
    - "Simulation log fixtures and settings roots are resolved via shared support helpers."
    - "Fixture capture and manifest loading live in a single support module."
    - "Fixture capture tests import support helpers without legacy fixture modules."
  artifacts:
    - path: "tests/support/paths.py"
      provides: "Shared repo/tests/fixtures/settings path resolution"
    - path: "tests/support/fixtures.py"
      provides: "FixtureEntry, load_manifest, capture_fixture, swap_settings_profile"
    - path: "tests/simulation_logs/parse.py"
      provides: "Fixture parsing using shared paths"
  key_links:
    - from: "tests/simulation_logs/parse.py"
      to: "tests/support/paths.py"
      via: "fixture/settings root helpers"
      pattern: "support\.paths"
    - from: "tests/e2e/test_simulation_log_fixtures.py"
      to: "tests/support/fixtures.py"
      via: "fixture helper import"
      pattern: "support\.fixtures"
---

<objective>
Centralize simulation log fixture path resolution and capture helpers in shared support modules, and wire existing tests to them.

Purpose: Establish a single source of truth for fixture and settings paths before directory moves.
Output: Support modules for paths/fixtures and updated imports in fixture parsing and capture tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-test-suite-refactor/08-CONTEXT.md
@.planning/phases/08-test-suite-refactor/08-RESEARCH.md

@tests/fixtures/simulation/capture.py
@tests/simulation_logs/parse.py
@tests/e2e/test_simulation_log_fixtures.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared path utilities and update parser paths</name>
  <files>tests/support/__init__.py, tests/support/paths.py, tests/simulation_logs/parse.py</files>
  <action>
Create `tests/support/paths.py` with explicit Path helpers for repo root, tests root, simulation fixture root,
manifest path, and settings profile root (from `tests/e2e/configs`). Add `tests/support/__init__.py` so the
support module is importable. Update `tests/simulation_logs/parse.py` to use the shared path helpers instead of
local `Path(__file__).parents[...]` calculations. Keep behavior unchanged aside from path resolution.
  </action>
  <verify>uv run python -c "from tests.support import paths; print(paths.simulation_fixtures_root())"</verify>
  <done>Path helper functions return valid Path objects and `parse.py` references them.</done>
</task>

<task type="auto">
  <name>Task 2: Move fixture helpers into support and rewire capture tests</name>
  <files>
tests/support/fixtures.py, tests/e2e/test_simulation_log_fixtures.py,
tests/fixtures/simulation/capture.py, tests/fixtures/simulation/__init__.py
  </files>
  <action>
Create `tests/support/fixtures.py` by moving `FixtureEntry`, `load_manifest`, `capture_fixture`, and
`swap_settings_profile` from `tests/fixtures/simulation/capture.py`. Update the helper code to use
`tests.support.paths` for repo root, fixture root, manifest path, and settings profile root. Update
`tests/e2e/test_simulation_log_fixtures.py` to import from `tests.support.fixtures` and use support path helpers
instead of local fixture root constants. Remove `tests/fixtures/simulation/capture.py` and
`tests/fixtures/simulation/__init__.py` to avoid legacy import paths.
  </action>
  <verify>uv run pytest tests/e2e/test_simulation_log_fixtures.py -q</verify>
  <done>Fixture capture tests import from `tests.support.fixtures`, and legacy fixture helper modules are removed.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/e2e/test_simulation_log_fixtures.py -q`
- `uv run pytest tests/test_simulation_log_parsing.py -q`
</verification>

<success_criteria>
- Shared path utilities exist in `tests/support/paths.py` and are used by fixture parsing.
- Fixture capture and manifest helpers live in `tests/support/fixtures.py` with updated imports.
</success_criteria>

<output>
After completion, create `.planning/phases/08-test-suite-refactor/08-01-SUMMARY.md`
</output>
