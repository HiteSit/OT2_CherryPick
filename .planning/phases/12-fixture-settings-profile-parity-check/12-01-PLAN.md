---
phase: 12-fixture-settings-profile-parity-check
plan: 01
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - tests/support/fixtures.py
  - tests/integration/simulation_logs/test_simulation_log_fixtures.py
autonomous: true

must_haves:
  truths:
    - "Every fixture in manifest.json has metadata.json with matching settings_profile."
    - "Settings profile mismatches fail fast with a fixture-specific message."
    - "Parity checks run for all manifest fixtures during simulation log fixture tests."
  artifacts:
    - path: "tests/support/fixtures.py"
      provides: "Helper to compare manifest vs metadata settings_profile for fixtures"
    - path: "tests/integration/simulation_logs/test_simulation_log_fixtures.py"
      provides: "Manifest-wide parity test using captured fixture metadata"
  key_links:
    - from: "tests/integration/simulation_logs/test_simulation_log_fixtures.py"
      to: "tests/support/fixtures.py"
      via: "assert_settings_profile_parity helper"
      pattern: "assert_settings_profile_parity"
    - from: "tests/integration/simulation_logs/test_simulation_log_fixtures.py"
      to: "tests/integration/simulation_logs/fixtures/manifest.json"
      via: "load_manifest parametrization"
      pattern: "load_manifest\\(\\)"
    - from: "tests/support/fixtures.py"
      to: "tests/integration/simulation_logs/fixtures/*/metadata.json"
      via: "metadata.json read"
      pattern: "metadata\\.json"
---

<objective>
Add a settings_profile parity check so manifest fixture intent cannot drift from captured metadata used for parsing and normalization.

Purpose: Prevent silent mismatches between expected transfer settings and parsed fixture settings.
Output: Fixture helper + integration test that assert manifest/metadata settings_profile parity across all fixtures.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fixture-settings-profile-parity-check/12-RESEARCH.md
@tests/integration/simulation_logs/fixtures/manifest.json
@tests/support/fixtures.py
@tests/integration/simulation_logs/test_simulation_log_fixtures.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fixture metadata parity helper</name>
  <files>tests/support/fixtures.py</files>
  <action>
Add a helper to load fixture metadata and compare settings_profile against FixtureEntry.settings_profile.
Introduce load_fixture_metadata(fixture_id) that reads metadata.json from FIXTURES_DIR / fixture_id and raises FileNotFoundError with the fixture id when missing.
Add assert_settings_profile_parity(entry, metadata=None) that reads metadata (if not provided), checks metadata settings_profile is present, and raises AssertionError with fixture_id, manifest settings_profile, and metadata settings_profile on mismatch.
Use support_paths.simulation_fixtures_root() via FIXTURES_DIR and stdlib json only.
  </action>
  <verify>uv run pytest tests/integration/simulation_logs/test_simulation_log_fixtures.py</verify>
  <done>fixtures.py exposes a parity helper that fails fast on missing or mismatched settings_profile values.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce settings_profile parity for all fixtures</name>
  <files>tests/integration/simulation_logs/test_simulation_log_fixtures.py</files>
  <action>
Import assert_settings_profile_parity from tests.support.fixtures.
Add a parametrized test over load_manifest() entries (ids=fixture_id) that calls _load_fixture_outputs(entry) to ensure metadata.json exists, then assert_settings_profile_parity(entry, metadata).
Ensure the assertion message includes the fixture id and both settings_profile values via the helper.
Keep the test colocated with existing fixture capture tests and reuse the manifest parametrization pattern.
  </action>
  <verify>uv run pytest tests/integration/simulation_logs/test_simulation_log_fixtures.py</verify>
  <done>All manifest fixtures assert parity between manifest and metadata settings_profile values during fixture tests.</done>
</task>

</tasks>

<verification>
- uv run pytest tests/integration/simulation_logs/test_simulation_log_fixtures.py
</verification>

<success_criteria>
- Every manifest fixture has matching settings_profile values between manifest.json and metadata.json.
- Parity mismatches fail with a fixture-specific message showing both settings_profile values.
- Fixture parity validation runs as part of the simulation log fixture test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/12-fixture-settings-profile-parity-check/12-01-SUMMARY.md`
</output>
