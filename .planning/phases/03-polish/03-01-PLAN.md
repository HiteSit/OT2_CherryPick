---
phase: 03-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gui/frontend/src/components/wizard/csv/CsvEditor.tsx
autonomous: true

must_haves:
  truths:
    - "User sees disabled dropdown with 'No CSV files found' when directory is empty"
    - "Upload CSV button is visually emphasized when dropdown is empty"
    - "Rest of editor remains fully functional for manual CSV creation"
  artifacts:
    - path: "src/gui/frontend/src/components/wizard/csv/CsvEditor.tsx"
      provides: "Empty state detection and conditional UI rendering"
      contains: "No CSV files found"
  key_links:
    - from: "CsvEditor.tsx"
      to: "csvListQuery.data?.files"
      via: "isEmpty derivation"
      pattern: "isEmpty.*csvListQuery"
---

<objective>
Implement graceful empty state handling when no CSV files exist in the directory.

Purpose: Provide clear feedback when the CSV directory is empty while keeping the editor fully functional for manual CSV creation.
Output: Modified CsvEditor with disabled dropdown showing "No CSV files found" and emphasized Upload button when empty.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polish/03-CONTEXT.md
@src/gui/frontend/src/components/wizard/csv/CsvEditor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add empty state detection and disabled dropdown</name>
  <files>src/gui/frontend/src/components/wizard/csv/CsvEditor.tsx</files>
  <action>
Add isEmpty derivation after csvOptions useMemo:

```typescript
const isEmpty = csvListQuery.data !== undefined && csvListQuery.data.files.length === 0
```

Modify the Select component:
1. Add `disabled` prop: `disabled={csvListQuery.isLoading || isEmpty}`
2. Change `placeholder` to be conditional: `placeholder={isEmpty ? "No CSV files found" : "Choose a file or type to search"}`

The key change is that when isEmpty is true:
- Dropdown is disabled (user cannot interact)
- Placeholder clearly communicates why (no files exist)
- Dropdown still renders (not hidden) so UI layout remains consistent

Do NOT change any other part of the editor - the rest must remain functional.
  </action>
  <verify>
TypeScript compiles: `cd /mnt/d/Amadteus_Main/OpenTron/OT2_CherryPick/src/gui/frontend && npm run build`
  </verify>
  <done>
Select dropdown shows "No CSV files found" and is disabled when csvListQuery returns empty files array; dropdown is enabled and shows normal placeholder when files exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Emphasize Upload CSV button when dropdown is empty</name>
  <files>src/gui/frontend/src/components/wizard/csv/CsvEditor.tsx</files>
  <action>
Modify the FileInput component to use conditional variant based on isEmpty:

Change from:
```tsx
<FileInput
  label="Upload CSV"
  placeholder="Choose file"
  leftSection={<IconUpload size={14} />}
  onChange={handleFileUpload}
  accept=".csv"
  clearable
  style={{ flex: 1, maxWidth: 200 }}
/>
```

To:
```tsx
<FileInput
  label="Upload CSV"
  placeholder="Choose file"
  leftSection={<IconUpload size={14} />}
  onChange={handleFileUpload}
  accept=".csv"
  clearable
  variant={isEmpty ? "filled" : "default"}
  style={{ flex: 1, maxWidth: 200 }}
/>
```

The `variant="filled"` provides visual emphasis (solid background) when empty, drawing user attention to the upload action. When files exist, use default styling.

Verify that Add Row, Remove Row, filename input, Save to workspace, tabs, spreadsheet, and textarea are unchanged and remain fully functional.
  </action>
  <verify>
1. TypeScript compiles: `cd /mnt/d/Amadteus_Main/OpenTron/OT2_CherryPick/src/gui/frontend && npm run build`
2. Manual verification: Start dev server and verify UI behavior with empty and non-empty CSV directories
  </verify>
  <done>
FileInput shows filled variant (emphasized) when no CSV files exist; shows default variant when files exist. All other editor functionality (Add Row, Remove Row, filename, tabs, spreadsheet, text) works unchanged.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. When CSV directory is empty:
   - Select dropdown is disabled
   - Select placeholder shows "No CSV files found"
   - FileInput has filled variant (visually emphasized)
   - All editor functionality works (Add Row, Remove Row, tabs, etc.)
3. When CSV directory has files:
   - Select dropdown is enabled
   - Select placeholder shows "Choose a file or type to search"
   - FileInput has default variant
   - All functionality works as before Phase 3
</verification>

<success_criteria>
- Empty state is visually communicated through disabled dropdown and "No CSV files found" placeholder
- Upload CSV button is emphasized when dropdown is empty (filled variant)
- Editor remains fully functional for manual CSV creation regardless of empty state
- No regressions to existing Phase 1 and Phase 2 functionality
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish/03-01-SUMMARY.md`
</output>
