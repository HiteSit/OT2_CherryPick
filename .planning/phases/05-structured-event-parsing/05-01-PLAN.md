---
phase: 05-structured-event-parsing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/simulation_logs/models.py
  - tests/simulation_logs/__init__.py
  - tests/simulation_logs/adapters/__init__.py
  - tests/simulation_logs/adapters/v8_7_0.py
  - tests/test_simulation_log_adapters.py
autonomous: true
must_haves:
  truths:
    - "Adapter parses opentrons_simulate 8.7.0 action lines into typed raw events."
    - "Aspirate, dispense, tip pickup/drop, and mix lines are captured with well, slot, and volume values."
    - "Unknown log lines are skipped without failing the parse."
  artifacts:
    - path: "tests/simulation_logs/models.py"
      provides: "Raw event dataclasses and parse result containers"
    - path: "tests/simulation_logs/adapters/v8_7_0.py"
      provides: "Regex-based parser for opentrons_simulate 8.7.0 logs"
    - path: "tests/test_simulation_log_adapters.py"
      provides: "Adapter coverage against fixture stdout"
  key_links:
    - from: "tests/simulation_logs/adapters/v8_7_0.py"
      to: "tests/fixtures/simulation/basic-single_x1/stdout.txt"
      via: "regex match on action lines"
      pattern: "Aspirating .* uL"
    - from: "tests/test_simulation_log_adapters.py"
      to: "tests/simulation_logs/adapters/v8_7_0.py"
      via: "parse_text invocation"
      pattern: "parse_text"
---

<objective>
Define simulation log event models and parse opentrons_simulate 8.7.0 action lines into raw events.

Purpose: Establish the core adapter that extracts structured actions from baseline fixtures.
Output: Raw event models, a v8.7.0 adapter, and tests that validate parsing on fixture logs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-structured-event-parsing/05-CONTEXT.md
@.planning/phases/05-structured-event-parsing/05-RESEARCH.md
@tests/fixtures/simulation/basic-single_x1/stdout.txt
@tests/fixtures/simulation/distribution-multi/stdout.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create raw event models and adapter package scaffolding</name>
  <files>
tests/simulation_logs/models.py
tests/simulation_logs/__init__.py
tests/simulation_logs/adapters/__init__.py
  </files>
  <action>
Create a new tests/simulation_logs package with dataclasses for raw parsing events and parse results. Define frozen dataclasses for raw events: labware_load, tip_pickup, tip_drop, aspirate, dispense, and mix. Include fields for sequence index, source stream (stdout or stderr), labware_display (raw name), labware_slot, well, volume_ul, and rate_ul_s where applicable. Add a ParseWarning (line, reason) and ParseResult (events, warnings) container. Export the raw event classes and ParseResult in tests/simulation_logs/__init__.py and adapters/__init__.py should remain minimal (no registry yet).
  </action>
  <verify>Open the new module and confirm each event dataclass includes sequence + slot + well fields, and ParseResult exposes events + warnings.</verify>
  <done>Raw event models are defined and importable from tests/simulation_logs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement v8.7.0 adapter parsing with fixture-backed tests</name>
  <files>
tests/simulation_logs/adapters/v8_7_0.py
tests/test_simulation_log_adapters.py
  </files>
  <action>
Implement a v8_7_0 adapter with regex patterns for action lines: Picking up tip, Dropping tip (trash and rack), Aspirating, Dispensing, and a best-effort Mix pattern (if present). The parse function should strip leading whitespace before matching, parse floats for volume/rate, and return a ParseResult with events in input order plus warnings for unmatched non-empty lines. Add tests that load fixture stdout for basic-single_x1 and distribution-multi, call the adapter parse function, and assert at least one tip pickup, aspirate, dispense, and tip drop event exist with expected slot/well/volume values. Include an assertion that indented substeps in distribution logs are parsed (no missing aspirate/dispense).
  </action>
  <verify>uv run pytest tests/test_simulation_log_adapters.py</verify>
  <done>Adapter parses fixture logs into raw events and tests validate the core action patterns.</done>
</task>

</tasks>

<verification>
- Adapter tests pass and confirm aspirate/dispense/tip actions are parsed from fixtures.
- ParseResult includes warnings for unknown lines without raising.
</verification>

<success_criteria>
- PARSE-01 groundwork: core action lines are parsed into typed raw events.
- Adapter is ready to be wired into version selection and normalization.
</success_criteria>

<output>
After completion, create `.planning/phases/05-structured-event-parsing/05-01-SUMMARY.md`
</output>
