---
phase: 05-structured-event-parsing
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - tests/simulation_logs/normalize.py
  - tests/simulation_logs/parse.py
  - tests/simulation_logs/adapters/__init__.py
  - tests/simulation_logs/__init__.py
  - tests/test_simulation_log_parsing.py
autonomous: true
must_haves:
  truths:
    - "Parser selects a log adapter using simulator_version metadata."
    - "Normalized events include labware_id, labware_slot, and pipette_id for actionable assertions."
    - "Synthetic labware load events are emitted from settings to cover missing log lines."
  artifacts:
    - path: "tests/simulation_logs/normalize.py"
      provides: "Settings-based enrichment and labware load synthesis"
    - path: "tests/simulation_logs/parse.py"
      provides: "Adapter registry selection and fixture parsing entrypoint"
    - path: "tests/test_simulation_log_parsing.py"
      provides: "End-to-end parsing coverage on simulation fixtures"
  key_links:
    - from: "tests/simulation_logs/parse.py"
      to: "tests/fixtures/simulation/<fixture-id>/metadata.json"
      via: "load metadata and select adapter"
      pattern: "simulator_version"
    - from: "tests/simulation_logs/normalize.py"
      to: "tests/e2e/configs/<profile>/settings.toml"
      via: "slot and pipette mapping"
      pattern: "working_plate"
---

<objective>
Normalize parsed events with settings context and select adapters by simulator version.

Purpose: Deliver version-aware, fully identified events for downstream validation tests.
Output: Adapter registry + parse entrypoint, normalization utilities, and end-to-end parsing tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-structured-event-parsing/05-CONTEXT.md
@.planning/phases/05-structured-event-parsing/05-RESEARCH.md
@tests/fixtures/simulation/manifest.json
@tests/fixtures/simulation/basic-single_x1/metadata.json
@tests/e2e/configs/single_X1/settings.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add normalization utilities and adapter registry</name>
  <files>
tests/simulation_logs/normalize.py
tests/simulation_logs/parse.py
tests/simulation_logs/adapters/__init__.py
tests/simulation_logs/__init__.py
  </files>
  <action>
Implement normalize.py with helpers to load settings.toml using tomllib, map deck slots to labware_id, and map tiprack slots to pipette_id from working_plate entries. Create synthetic labware_load events for each working_plate entry (sequence order before parsed events). Normalize raw events by attaching labware_id, labware_slot, and pipette_id; for aspirate/dispense/mix, infer pipette_id from the most recent tip_pickup event (fallback to the only configured pipette if unambiguous). Raise a ValueError if a raw event references a slot that is not in the settings map so missing identifiers are surfaced. In parse.py, add an adapter registry keyed by simulator_version with v8_7_0 as the default; expose select_adapter(metadata) and parse_fixture(fixture_id) that loads stdout/stderr/metadata, parses both streams, merges results preserving order, and normalizes with the settings profile from metadata (tests/e2e/configs/<profile>/settings.toml). Update __init__.py to export parse_fixture and the normalized event types.
  </action>
  <verify>Run a quick import in a Python REPL or test to ensure parse_fixture returns normalized events with labware_id and pipette_id populated.</verify>
  <done>Version selection and normalization utilities are implemented and produce fully identified events.</done>
</task>

<task type="auto">
  <name>Task 2: Add end-to-end parsing tests against fixtures</name>
  <files>tests/test_simulation_log_parsing.py</files>
  <action>
Create pytest coverage that calls parse_fixture for at least basic-single_x1 and distribution-multi. Assert that normalized events include labware_load events for each working_plate entry, at least one tip_pickup, aspirate, dispense, and tip_drop event, and that these events include non-empty labware_id, labware_slot, and pipette_id fields. Add a test that select_adapter chooses the v8_7_0 adapter based on metadata simulator_version. Add a test for unknown simulator_version that returns a warning and still produces an empty event list (no crash).
  </action>
  <verify>uv run pytest tests/test_simulation_log_parsing.py</verify>
  <done>Tests prove adapter selection and normalization from fixtures into structured events.</done>
</task>

</tasks>

<verification>
- Parsing tests confirm adapter selection, synthetic labware loads, and full identifiers on events.
- Unknown simulator versions emit warnings without raising.
</verification>

<success_criteria>
- PARSE-01 satisfied: tests can load fixtures and access structured events for labware load, tip actions, aspirate, dispense, and mix.
- COMP-01 satisfied: adapter selection is version-aware and test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/05-structured-event-parsing/05-02-SUMMARY.md`
</output>
