---
phase: 10-fixture-downstream-coverage
plan: 01
type: execute
wave: 1
depends_on: ["09-01"]
files_modified:
  - tests/unit/transfer_mapping/test_transfer_mapping.py
  - tests/unit/transfer_mapping/test_transfer_policies.py
autonomous: true

must_haves:
  truths:
    - "All fixtures listed in the simulation manifest run through downstream match + coverage assertions."
    - "Success fixtures assert full transfer coverage; expected-failure fixtures assert non-success matching."
    - "All fixtures run policy evaluation without crashing, with errors only allowed on expected failures."
  artifacts:
    - path: "tests/unit/transfer_mapping/test_transfer_mapping.py"
      provides: "Manifest-driven downstream match + coverage validation for fixtures"
    - path: "tests/unit/transfer_mapping/test_transfer_policies.py"
      provides: "Manifest-driven policy evaluation coverage for fixtures"
  key_links:
    - from: "tests/unit/transfer_mapping/test_transfer_mapping.py"
      to: "tests/support/fixtures.py"
      via: "load_manifest() fixture parametrization"
      pattern: "load_manifest\(\)"
    - from: "tests/unit/transfer_mapping/test_transfer_mapping.py"
      to: "tests/support/simulation.py"
      via: "parse_fixture_entry + build_expected_transfers_for_entry"
      pattern: "parse_fixture_entry|build_expected_transfers_for_entry"
    - from: "tests/unit/transfer_mapping/test_transfer_policies.py"
      to: "tests/unit/simulation_logs/policies.py"
      via: "evaluate_policies on parsed events"
      pattern: "evaluate_policies"
---

<objective>
Add manifest-driven downstream tests so every captured simulation fixture is parsed, matched, and policy-evaluated with explicit expected-failure handling.

Purpose: Close fixture coverage gaps for extreme-single_x1 and invalid-labware across parse/match/policy tests.
Output: Updated transfer mapping and policy tests that iterate all manifest fixtures with clear assertions.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-fixture-downstream-coverage/10-RESEARCH.md
@tests/integration/simulation_logs/fixtures/manifest.json
@tests/support/fixtures.py
@tests/support/simulation.py
@tests/unit/transfer_mapping/test_transfer_mapping.py
@tests/unit/transfer_mapping/test_transfer_policies.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add manifest-wide downstream match + coverage assertions</name>
  <files>tests/unit/transfer_mapping/test_transfer_mapping.py</files>
  <action>
Add a new parametrized test that iterates every FixtureEntry from load_manifest() (use ids=fixture_id).
For each entry: build expected transfers via build_expected_transfers_for_entry(entry), parse events via parse_fixture_entry(entry), and run match_transfers + compute_row_coverage.
For expect_failure fixtures, assert match.success is False and coverage.covered_rows < coverage.total_rows; include format_transfer_report(match, expectations) in assertion messages for diagnostics.
For non-failure fixtures, assert match.success is True and coverage.covered_rows == coverage.total_rows, with format_transfer_report/format_row_coverage in assertion messages.
Do not enable allow_extra_events; rely on default strict matching to preserve Phase 6 behavior.
  </action>
  <verify>uv run pytest tests/unit/transfer_mapping/test_transfer_mapping.py</verify>
  <done>Every manifest fixture is exercised by match + coverage assertions with explicit expect_failure handling.</done>
</task>

<task type="auto">
  <name>Task 2: Add manifest-wide policy evaluation coverage</name>
  <files>tests/unit/transfer_mapping/test_transfer_policies.py</files>
  <action>
Add a new parametrized test over load_manifest() entries (ids=fixture_id) that uses build_fixture_context(entry) to get expected, parsed, csv_path, settings, then runs match_transfers and evaluate_policies.
For non-failure fixtures, assert result.errors is empty and include result.summary() on failure; warnings are allowed.
For expect_failure fixtures, assert match.success is False and still call evaluate_policies; do not require errors to be empty, but ensure result.summary() is used in assertions so policy evaluation is exercised.
  </action>
  <verify>uv run pytest tests/unit/transfer_mapping/test_transfer_policies.py</verify>
  <done>All manifest fixtures run policy evaluation, with success fixtures error-free and expected-failure fixtures handled explicitly.</done>
</task>

</tasks>

<verification>
- uv run pytest tests/unit/transfer_mapping/test_transfer_mapping.py
- uv run pytest tests/unit/transfer_mapping/test_transfer_policies.py
</verification>

<success_criteria>
- All fixtures in manifest.json are included in downstream match + coverage tests.
- The invalid-labware fixture is validated as an expected failure (non-success match) rather than skipped.
- Policy evaluation runs for every fixture with errors only allowed on expected failures.
</success_criteria>

<output>
After completion, create `.planning/phases/10-fixture-downstream-coverage/10-01-SUMMARY.md`
</output>
