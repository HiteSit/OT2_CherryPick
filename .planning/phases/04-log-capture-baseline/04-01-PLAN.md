---
phase: 04-log-capture-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/simulation/manifest.json
  - tests/fixtures/simulation/capture.py
  - tests/fixtures/simulation/__init__.py
  - tests/fixtures/csvs/invalid_labware.csv
  - tests/e2e/configs/liquid_extreme/settings.toml
  - tests/e2e/test_simulation_log_fixtures.py
autonomous: true
must_haves:
  truths:
    - "Simulation fixture capture produces stdout/stderr logs for each fixture scenario."
    - "Simulation warnings or errors fail the fixture capture test with the offending output excerpt."
    - "Fixture metadata records CSV, settings profile, simulator version, and labware path used."
  artifacts:
    - path: "tests/fixtures/simulation/manifest.json"
      provides: "Fixture matrix for simulation capture"
    - path: "tests/fixtures/simulation/capture.py"
      provides: "simulate_protocol.sh wrapper with settings swapping and metadata capture"
    - path: "tests/e2e/test_simulation_log_fixtures.py"
      provides: "pytest coverage for log capture and error surfacing"
    - path: "tests/fixtures/simulation/<fixture-id>/metadata.json"
      provides: "Per-fixture metadata including versions and paths"
  key_links:
    - from: "tests/fixtures/simulation/capture.py"
      to: "simulate_protocol.sh"
      via: "subprocess.run(['bash','simulate_protocol.sh', csv_path])"
      pattern: "simulate_protocol.sh"
    - from: "tests/fixtures/simulation/capture.py"
      to: "settings.toml"
      via: "profile copy before capture"
      pattern: "shutil.copy2"
    - from: "tests/e2e/test_simulation_log_fixtures.py"
      to: "tests/fixtures/simulation/manifest.json"
      via: "load manifest to iterate fixtures"
      pattern: "manifest.json"
---

<objective>
Capture opentrons_simulate logs as reusable fixtures and fail tests on simulator warnings/errors.

Purpose: Establish a deterministic log baseline for later parsing and validation phases.
Output: Fixture manifest, capture helper, and pytest coverage with stored stdout/stderr + metadata.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-log-capture-baseline/04-CONTEXT.md
@.planning/phases/04-log-capture-baseline/04-RESEARCH.md
@tests/e2e/conftest.py
@simulate_protocol.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define fixture matrix and coverage assets</name>
  <files>
tests/fixtures/simulation/manifest.json
tests/fixtures/csvs/invalid_labware.csv
tests/e2e/configs/liquid_extreme/settings.toml
  </files>
  <action>
Create a fixture manifest JSON listing fixture_id, csv_path (relative), settings_profile, and expect_failure. Use existing CSVs for mode boundaries and labware variety (example_basic with single_X1 + multi_X1, example_multi_mode with multi, example_distribution with multi, example_home_control with single_X1, fill_analytics_plate with fill_analytics). Add a liquid-handling extreme fixture by creating a liquid_extreme settings profile (copy single_X1, enable pre_aspirate_contact + post_aspirate_wick, set post_aspirate delay to 3, push_out volume to 8, keep deck layout intact). Add an intentional failure CSV under tests/fixtures/csvs with a non-existent labware_id in Source/Dest so helper/simulation fails; mark expect_failure true in the manifest. Keep fixture IDs stable and descriptive (e.g., basic-single_x1, multi-multi, extreme-single_x1, invalid-labware).
  </action>
  <verify>Confirm manifest entries map to real CSVs/settings and the failure CSV triggers helper validation when run manually.</verify>
  <done>Fixture manifest exists with coverage for mode boundaries, labware variety, liquid extremes, and a failure case.</done>
</task>

<task type="auto">
  <name>Task 2: Implement simulation fixture capture helper</name>
  <files>
tests/fixtures/simulation/capture.py
tests/fixtures/simulation/__init__.py
  </files>
  <action>
Implement a capture helper that loads the manifest, resolves repo root, parses simulate_protocol.sh to read MACHINE_CONFIG and LABWARE_PATH_WIN for that case, applies LABWARE_PATH_WIN_OVERRIDE if set, converts the Windows path to WSL (wslpath if available, else manual conversion like the script), and validates the labware directory exists and contains at least one *.json file. Add a settings swap context manager that backs up repo-root settings.toml and copies the selected profile from tests/e2e/configs/<profile>/settings.toml before each run, restoring after. Provide a capture_fixture function that runs ["bash", "simulate_protocol.sh", csv_path] with subprocess.run(capture_output=True, text=True, cwd=repo root), records stdout/stderr/returncode, and writes stdout.txt, stderr.txt, and metadata.json (fixture_id, csv, settings_profile, simulator_version from `opentrons_simulate -v`, labware_path, command, returncode, timestamp). Keep logs raw (no normalization) and always write metadata even on failure.
  </action>
  <verify>Run a single fixture capture in isolation and confirm stdout.txt, stderr.txt, and metadata.json are written with expected fields.</verify>
  <done>Capture helper can execute simulate_protocol.sh, swap settings profiles safely, and persist raw logs + metadata per fixture.</done>
</task>

<task type="auto">
  <name>Task 3: Add pytest coverage for fixture capture and error surfacing</name>
  <files>tests/e2e/test_simulation_log_fixtures.py</files>
  <action>
Create a pytest module that parametrizes over the manifest and uses the capture helper. Mark with @pytest.mark.requires_simulation and @pytest.mark.pipeline_test. If fixture output files already exist and OT2_REFRESH_SIM_FIXTURES is not set, load existing stdout/stderr/metadata instead of re-running capture; otherwise, call capture_fixture to refresh. For non-failure fixtures, assert returncode == 0 and fail if stdout/stderr contains warning/error markers (case-insensitive match for "warning" or "error"), raising AssertionError that includes fixture_id and a trimmed excerpt of stdout/stderr. For expect_failure fixtures, assert returncode != 0 and still persist outputs. Ensure failures surface the offending output in the assertion message.
  </action>
  <verify>uv run pytest tests/e2e/test_simulation_log_fixtures.py -m requires_simulation</verify>
  <done>Fixture capture tests either reuse stored logs or refresh them and fail loudly on warnings/errors with clear context.</done>
</task>

</tasks>

<verification>
- Fixture capture test creates fixture directories with stdout/stderr/metadata for each manifest entry.
- Running the capture test fails on any simulator warning/error with output context.
</verification>

<success_criteria>
- LOG-01 satisfied: stored stdout/stderr fixtures are generated or reused for repeatable runs.
- DIAG-01 satisfied: warnings/errors trigger test failures with captured log excerpts.
</success_criteria>

<output>
After completion, create `.planning/phases/04-log-capture-baseline/04-01-SUMMARY.md`
</output>
