Phase 1-3 summary: FastMCP server + generate_protocol tool; TomlHandler with set/batch/append backing update_settings/apply_liquid_preset and add_labware_definition; simulation + validation helpers/tools; new full_workflow tool chaining validation → generation → optional simulation alongside unit/integration pytest suites. Docs reviewed (fastmcp, mcp-use). Tests run via `pixi run pytest` (full run subject to sandbox cache quirk).