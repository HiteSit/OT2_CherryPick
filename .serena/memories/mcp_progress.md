Phases 1-3 progress: bootstrapped FastMCP server (stderr logging, repo-root cwd) with generate_protocol tool; delivered TomlHandler (read/set/batch/append + backups), config tools (update_settings, apply_liquid_preset), and labware addition tool. Phase 3 now includes CSV helpers (generate_csv_template tool, list_csv_files utility + files://csvs resource), simulation logging (core writes logs/last_simulation.json, simulate_protocol tool returns log path, logs://last-simulation resource), validation pipeline, and full_workflow orchestration. Tests span csv/simulation/resource/mcp registration via `pixi run pytest` targeted suites. Docs for FastMCP and mcp-use reviewed to keep implementation aligned.