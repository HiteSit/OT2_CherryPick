Phase 1-2 work so far: bootstrapped FastMCP server with logging/cwd control and generate_protocol tool; implemented TomlHandler with read/set/batch/append plus backup writes; exposed update_settings + apply_liquid_preset config tools and add_labware_definition labware tool; registered settings/labware resources. Phase 3 kickoff: added simulation core wrapper around opentrons_simulate, simulate_protocol MCP tool, and pytest coverage across new helpers. Tests run via `pixi run pytest`.